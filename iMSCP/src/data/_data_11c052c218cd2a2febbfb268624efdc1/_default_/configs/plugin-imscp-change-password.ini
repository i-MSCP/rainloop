; RainLoop Webmail plugin (imscp-change-password)

[plugin]
pdo_dsn = "mysql:host={DATABASE_HOST};port={DATATABASE_PORT};dbname={DATABASE_NAME}"
user = "{DATABASE_USER}"
password = "{DATABASE_PASSWORD}"
allowed_emails = "*"
